---
interface Props {
    id: string
    img: string
    success: boolean
    flightNumber: number
    details: null | string
}

const { success, flightNumber, img, id, details } = Astro.props
const successText = success ? "Éxito" : "Fracaso"
---

<section
    class="scale-100 rounded-lg border shadow-md bg-gray-800 border-gray-700"
>
    <picture class="flex justify-center p-4">
        <img src={img} alt={`Patch for launc ${id}`} class="mb-5 rounded-lg">
    </picture>
    <header class="p-4 justify-center rounded-lg">
        <span 
            class:list={[
                "text-xs font-semibold mr-2 px-2 py-0.5 rounded",
                {
                    "bg-green-200 text-green-900": success,
                    "bg-red-200 text-red-900": !success
                }
            ]}            
        >
                {successText}
        </span>

        <h2 class="my-2 text-2xl font-bold tracking-tight text-white" >
            Flight #{flightNumber}
        </h2>

        <p  class="mb-4 font-light text-gray-400">
            {(details !== null && details.length > 100) ? details.slice(0, 100)+" ... " : details}
        </p>
    </header>
</a>
<style>
    section {
        background: hwb(223 16% 75%);
    }
</style>